import 'package:flutter/material.dart';

class practice_practice extends StatelessWidget {
  const practice_practice({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(
        "hello world",
        style: TextStyle(
          fontSize: 30,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}